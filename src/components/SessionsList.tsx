import React from 'react';

type Props = {
  className?: string;
};

const SessionsList = ({ className }: Props): JSX.Element => {
  return (
    <div>
      <div className="bg-[#F00] text-slate-800">SessionsList2</div>
    </div>
  );
};

export default SessionsList;
